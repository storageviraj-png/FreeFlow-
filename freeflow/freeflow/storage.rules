rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /freeflow-images/{allPaths=**} {
      allow read: if true;                 // output.html loads images unauthenticated via public URL
      allow write: if request.auth != null; // only signed-in (anonymous) operators can upload
    }
  }
}
